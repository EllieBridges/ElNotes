import TextField from '@mui/material/TextField';

export default function () {
    return (
        <TextField
            id="outlined-multiline-flexible"
            label="Multiline"
            multiline
            maxRows={10}
        />)
}
